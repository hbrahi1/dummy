pipeline {
    agent any
    stages {
        stage('Hello_Webhook') {
            steps {
                sh "echo Hello Pipeline from Github to test webhooks and jenkins pipelines!"
            }
        }
    }
}
